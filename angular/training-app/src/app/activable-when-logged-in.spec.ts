import { ActionableWhenLoggedIn } from './actionable-when-logged-in.service';

describe('ActivableWhenLoggedIn', () => {
  it('should create an instance', () => {
    expect(new ActionableWhenLoggedIn()).toBeTruthy();
  });
});
